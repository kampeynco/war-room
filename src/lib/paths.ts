import path from "node:path";

// Docs live inside the project under docs/
export const KAMPEYN_DOCS_DIR = path.join(process.cwd(), "docs", "kampeyn");

// Organize SD docs
export const ORGANIZESD_DIR = path.join(process.cwd(), "docs", "organizesd");
